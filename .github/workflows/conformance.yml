name: conformance

on:
  push:
    branches: [main]
    paths:
      - "spec/**"
      - "langs/**/tests/conformance/**"
      - "langs/csharp/tests/VMx.Conformance.Tests/**"
      - "langs/python/pyproject.toml"
      - "tools/**"
      - ".github/workflows/conformance.yml"
  pull_request:
    paths:
      - "spec/**"
      - "langs/**/tests/conformance/**"
      - "langs/csharp/tests/VMx.Conformance.Tests/**"
      - "langs/python/pyproject.toml"
      - "tools/**"
      - ".github/workflows/conformance.yml"
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync Python dependencies (for pytest)
        run: uv --project langs/python sync --all-extras

      - name: Ruff lint tools/
        # The conformance-coverage tool lives at the repo root (outside the
        # langs/python lint scope) but is CI-critical; lint it here so style
        # regressions in this script don't slip past every other workflow.
        run: uv --project langs/python run ruff check tools/

      - name: Run tool's own unit tests
        run: uv --project langs/python run pytest tools/tests/ -v

      - name: Enforce conformance coverage (csharp + python + typescript)
        # All three flavors (csharp / python / typescript) ship at v2.0.0. CI
        # enforces 100% conformance coverage (152 / 152) for all three
        # languages. The tool exits non-zero if any required language falls
        # below full coverage.
        run: uv --project langs/python run python tools/check-conformance-coverage.py --require csharp --require python --require typescript
