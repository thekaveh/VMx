"""VMx ViewModels for the notes showcase.

Importers reach individual VMs via fully-qualified module paths
(``notes_showcase.viewmodels.notebook_vm``, etc.) — the package keeps no
implicit re-exports so the dependency graph between VMs stays explicit.
"""
