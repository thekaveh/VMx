<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NotesShowcase.ViewModels"
             x:Class="NotesShowcase.Views.NotesListView"
             x:DataType="vm:NotesViewVM">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Margin="12">
        <!-- Search input (row 0) -->
        <TextBox Grid.Row="0"
                 x:Name="SearchInput"
                 AutomationProperties.Name="Search notes"
                 Watermark="Search…"
                 Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,8" />

        <!-- Starred-only toggle (row 1) -->
        <CheckBox Grid.Row="1"
                  Content="Starred only"
                  AutomationProperties.Name="Show starred notes only"
                  IsChecked="{Binding ShowStarredOnly, Mode=TwoWay}"
                  Margin="0,0,0,8" />

        <!-- Notes list (row 2) -->
        <ListBox Grid.Row="2"
                 x:Name="NotesList"
                 AutomationProperties.Name="Notes"
                 ItemsSource="{Binding VisibleItems}"
                 SelectedItem="{Binding Current, Mode=TwoWay}"
                 Background="Transparent"
                 Foreground="{StaticResource TextBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="★"
                                   Foreground="{StaticResource AccentBrush}"
                                   IsVisible="{Binding Starred}" />
                        <TextBlock Text="{Binding Title}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Pagination strip (row 3) -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Spacing="6"
                    Margin="0,8,0,0">
            <Button Content="⏮"
                    AutomationProperties.Name="First page"
                    Command="{Binding MoveToFirstPageCommand}" />
            <Button Content="◀"
                    AutomationProperties.Name="Previous page"
                    Command="{Binding MoveToPreviousPageCommand}" />
            <TextBlock VerticalAlignment="Center"
                       MinWidth="80"
                       TextAlignment="Center"
                       Text="{Binding PageLabel}" />
            <Button Content="▶"
                    AutomationProperties.Name="Next page"
                    Command="{Binding MoveToNextPageCommand}" />
            <Button Content="⏭"
                    AutomationProperties.Name="Last page"
                    Command="{Binding MoveToLastPageCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
