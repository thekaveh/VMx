// Placeholder entry point — the full SwiftUI app is implemented in a later task.
// (SwiftUI requires a different target configuration; this keeps the package
// resolving and the Core + Tests targets exercisable in CI.)
print("NotesShowcase")
