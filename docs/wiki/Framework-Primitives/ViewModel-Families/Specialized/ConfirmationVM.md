# ConfirmationVM

Use `ConfirmationVM` when a notification stays visible until the user approves,
rejects, or dismisses it.

## Key Difference

Unlike `NotificationVM`, timeout may drive UI decay but does not auto-resolve
the underlying decision.

## Related Pages

- \[[NotificationVM|Framework-Primitives/ViewModel-Families/Specialized/NotificationVM]\]
- \[[ModalVM|Framework-Primitives/ViewModel-Families/Specialized/ModalVM]\]
