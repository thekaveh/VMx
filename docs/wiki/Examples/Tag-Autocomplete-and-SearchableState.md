# Tag Autocomplete & SearchableState

The note form's tag suggestions are a compact example of `SearchableState<T>`
inside the flagship portfolio.

## Why It Matters

- shows the same helper on a lightweight string collection
- keeps search state in the VM layer instead of in the host view
- complements the broader notes-list filtering story

## Related Pages

- \[[Notes Workspace|Examples/Notes-Workspace]\]
- \[[State & Reactive Helpers|Framework-Primitives/State-and-Reactive-Helpers]\]
