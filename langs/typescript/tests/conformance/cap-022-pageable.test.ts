// Conformance tests: CAP-022 — IPageable capability contract surface.
// See spec/14-capabilities.md §2.10 and ADR-0023.

import { describe, it } from "vitest";

describe("CAP-022", () => {
  it.todo(
    "IPageable contract: mutable PageSize/CurrentPageIndex, derived PageCount/IsPagingEnabled, navigation no-ops at bounds",
  );
});
