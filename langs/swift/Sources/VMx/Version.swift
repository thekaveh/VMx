//
// Package version constants — parity with C# `VMxInfo.MinSpecVersion`,
// Python `vmx.__version__` / `__min_spec_version__`, and TypeScript
// `__version__` / `__minSpecVersion__`.
//
public enum VMxVersion {
    /// The package version.
    public static let current = "2.4.0"

    /// The minimum spec version this package implements.
    public static let minSpecVersion = "2.4.0"
}
